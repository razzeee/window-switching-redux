{
  "name": "window-switching-redux",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "pack": "gnome-extensions pack . -f -o dist $(printf ' --extra-source=%s' windowModel.js switcher*.js)"
  }
}
